# El perfil de produccion carga exclusivamente su archivo de entorno.
spring.config.import=optional:file:.env[.properties]

# La documentacion interactiva solo se publica en desarrollo y pruebas.
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
